// Find your createAppKit initialization (usually in main.jsx or a config file)
createAppKit({
  adapters: [wagmiAdapter],
  projectId: 'YOUR_PROJECT_ID',
  networks: [mainnet, arbitrum],
  metadata: metadata,
  features: {
    email: true, // This enables Email login
    socials: ['google', 'apple', 'x'], // This enables Gmail and Apple buttons
    emailShowWallets: true,
  }
})
